{
  "name": "tsdk-hackernews-api",
  "description": "HackerNews API with TypeScript",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "ready": "pnpm --filter=api sync-sdk",
    "start-publish": "cd hn-api-sdk && npm publish --access public --no-git-checks --provenance --registry=https://registry.npmjs.org"
  }
}
